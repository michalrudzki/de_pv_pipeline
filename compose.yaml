name: de_pv_data

services:
  python_scrap:
    image: python:3.12.10
    working_dir: /app
    volumes:
      - .:/app/

  # python pv_web_scrap.py
    command: >
      sh -c "pip install --no-cache-dir -r requirements.txt &&
            python weather_api.py &&
            pv_web_scrap.py"
